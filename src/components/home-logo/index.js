import { h, Component } from "preact";

import CalIcon from "../Icons/Cal";
class HomeLogo extends Component {
  render({}, {}) {
    return (
      <div>
        <CalIcon />
      </div>
    );
  }
}
